name: timeout workflow
on:
   workflow_dispatch:
      branches:
          - main
          - dev
          - production
jobs:
   name: step level timeout
   runs-on: ubuntu-latest
   steps:
      - run: |
         sleep 180
         timeout-minutes: 2
